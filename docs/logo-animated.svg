<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 900 420" width="900" height="420">
  <defs>
    <radialGradient id="glowPurple" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#8b7cf8" stop-opacity="0.4"/>
      <stop offset="100%" stop-color="#8b7cf8" stop-opacity="0"/>
    </radialGradient>
    <radialGradient id="glowPurpleBright" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#8b7cf8" stop-opacity="0.8"/>
      <stop offset="100%" stop-color="#8b7cf8" stop-opacity="0"/>
    </radialGradient>
    <style>
      /* =============================================
         12s CYCLE: Act 1 (0-5s), transition (5-6s),
         Act 2 (6-11s), transition (11-12s)
         ============================================= */

      /* --- Speech bubble text switching --- */

      /* Text A: "Customer health?" — visible Act 1, hidden Act 2 */
      @keyframes textA {
        0%   { opacity: 0; }
        2%   { opacity: 1; }
        40%  { opacity: 1; }   /* 5/12 ~ 41% */
        43%  { opacity: 0; }
        100% { opacity: 0; }
      }
      .text-a { animation: textA 12s ease infinite; }

      /* Text B: "Revenue by region?" — hidden Act 1, visible Act 2 */
      @keyframes textB {
        0%   { opacity: 0; }
        48%  { opacity: 0; }   /* 6/12 = 50% */
        51%  { opacity: 1; }
        90%  { opacity: 1; }   /* 11/12 ~ 91% */
        93%  { opacity: 0; }
        100% { opacity: 0; }
      }
      .text-b { animation: textB 12s ease infinite; }

      /* DB label switching: Gold in Act 1, Silver in Act 2 */
      .db-label-gold { animation: textA 12s ease infinite; }
      .db-label-silver { animation: textB 12s ease infinite; }

      /* Bottom row text with 0.2s offset = 1.67% */
      .text-a-bot { animation: textA 12s ease infinite; animation-delay: 0.2s; }
      .text-b-bot { animation: textB 12s ease infinite; animation-delay: 0.2s; }

      /* --- ACT 1: Top row dots (0s-5s) --- */

      /* Top request dot: agent(120) -> hub(450), starts at 0.5s */
      @keyframes topReq1 {
        0%    { cx: 120; opacity: 0; }
        4%    { cx: 120; opacity: 1; }    /* 0.5s */
        16.6% { cx: 450; opacity: 1; }   /* 2.0s */
        17.5% { cx: 450; opacity: 0; }
        100%  { opacity: 0; }
      }
      .top-req1 { animation: topReq1 12s linear infinite; }

      /* Top pass-through dot: hub(450) -> db(770), starts at 2.0s */
      @keyframes topPass1 {
        0%    { cx: 450; opacity: 0; }
        16.6% { cx: 450; opacity: 0; }   /* 2.0s */
        17%   { cx: 450; opacity: 1; }
        21%   { cx: 770; opacity: 1; }    /* 2.5s */
        22%   { cx: 770; opacity: 0; }
        100%  { opacity: 0; }
      }
      .top-pass1 { animation: topPass1 12s linear infinite; }

      /* Top response dot: db(770) -> agent(120), starts at 2.5s */
      @keyframes topResp1 {
        0%    { cx: 770; opacity: 0; }
        21%   { cx: 770; opacity: 0; }    /* 2.5s */
        22%   { cx: 770; opacity: 1; }
        33%   { cx: 120; opacity: 1; }    /* 4.0s */
        34%   { cx: 120; opacity: 0; }
        100%  { opacity: 0; }
      }
      .top-resp1 { animation: topResp1 12s linear infinite; }

      /* Top 200 OK label — Act 1 */
      @keyframes topOk1 {
        0%    { opacity: 0; }
        33%   { opacity: 0; }   /* 4.0s */
        35%   { opacity: 1; }
        41%   { opacity: 1; }   /* 5.0s */
        43%   { opacity: 0; }
        100%  { opacity: 0; }
      }
      .top-ok1 { animation: topOk1 12s ease infinite; }

      /* --- ACT 1: Bottom row dots (offset 0.2s) --- */

      /* Bot request dot: agent(120) -> rest(450), starts at 0.7s */
      @keyframes botReq1 {
        0%    { cx: 120; opacity: 0; }
        5.8%  { cx: 120; opacity: 1; }    /* 0.7s */
        18.3% { cx: 450; opacity: 1; }    /* 2.2s */
        19%   { cx: 450; opacity: 0; }
        100%  { opacity: 0; }
      }
      .bot-req1 { animation: botReq1 12s linear infinite; }

      /* Bot pass-through dot: rest(450) -> db(770), starts at 2.2s */
      @keyframes botPass1 {
        0%    { cx: 450; opacity: 0; }
        18.3% { cx: 450; opacity: 0; }    /* 2.2s */
        19%   { cx: 450; opacity: 1; }
        23%   { cx: 770; opacity: 1; }    /* 2.7s */
        24%   { cx: 770; opacity: 0; }
        100%  { opacity: 0; }
      }
      .bot-pass1 { animation: botPass1 12s linear infinite; }

      /* Bot response dot: db(770) -> agent(120), starts at 2.7s */
      @keyframes botResp1 {
        0%    { cx: 770; opacity: 0; }
        22.5% { cx: 770; opacity: 0; }    /* 2.7s */
        23%   { cx: 770; opacity: 1; }
        35%   { cx: 120; opacity: 1; }    /* 4.2s */
        36%   { cx: 120; opacity: 0; }
        100%  { opacity: 0; }
      }
      .bot-resp1 { animation: botResp1 12s linear infinite; }

      /* Bot 200 OK label — Act 1 */
      @keyframes botOk1 {
        0%    { opacity: 0; }
        35%   { opacity: 0; }   /* 4.2s */
        37%   { opacity: 1; }
        41%   { opacity: 1; }   /* 5.0s */
        43%   { opacity: 0; }
        100%  { opacity: 0; }
      }
      .bot-ok1 { animation: botOk1 12s ease infinite; }

      /* --- ACT 2: Top row dots (6s-11s) --- */

      /* Top request dot: agent(120) -> hub(450), starts at 6.5s */
      @keyframes topReq2 {
        0%    { cx: 120; opacity: 0; }
        54%   { cx: 120; opacity: 0; }    /* 6.5s */
        55%   { cx: 120; opacity: 1; }
        62.5% { cx: 450; opacity: 1; }    /* 7.5s */
        63.5% { cx: 450; opacity: 0; }
        100%  { opacity: 0; }
      }
      .top-req2 { animation: topReq2 12s linear infinite; }

      /* Top composed query dot: hub(450) -> db(770), starts at 8.0s */
      @keyframes topComposed2 {
        0%    { cx: 450; opacity: 0; }
        66.6% { cx: 450; opacity: 0; }    /* 8.0s */
        67%   { cx: 450; opacity: 1; }
        71%   { cx: 770; opacity: 1; }    /* 8.5s */
        72%   { cx: 770; opacity: 0; }
        100%  { opacity: 0; }
      }
      .top-composed2 { animation: topComposed2 12s linear infinite; }

      /* Top response dot: db(770) -> agent(120), starts at 8.5s */
      @keyframes topResp2 {
        0%    { cx: 770; opacity: 0; }
        71%   { cx: 770; opacity: 0; }    /* 8.5s */
        72%   { cx: 770; opacity: 1; }
        79%   { cx: 120; opacity: 1; }    /* 9.5s */
        80%   { cx: 120; opacity: 0; }
        100%  { opacity: 0; }
      }
      .top-resp2 { animation: topResp2 12s linear infinite; }

      /* Top 200 OK + "composed from Silver" label — Act 2 */
      @keyframes topOk2 {
        0%    { opacity: 0; }
        79%   { opacity: 0; }   /* 9.5s */
        81%   { opacity: 1; }
        91%   { opacity: 1; }   /* 11s */
        93%   { opacity: 0; }
        100%  { opacity: 0; }
      }
      .top-ok2 { animation: topOk2 12s ease infinite; }

      /* "composing..." label near hub during Act 2 */
      @keyframes composingLabel {
        0%    { opacity: 0; }
        62.5% { opacity: 0; }   /* 7.5s */
        64%   { opacity: 1; }
        66.6% { opacity: 1; }   /* 8.0s */
        68%   { opacity: 0; }
        100%  { opacity: 0; }
      }
      .composing-label { animation: composingLabel 12s ease infinite; }

      /* Hub glow brighter during Act 2 composing (7.5s-8.0s) */
      @keyframes hubGlowAct2 {
        0%    { opacity: 0; }
        62.5% { opacity: 0; }
        64%   { opacity: 1; }
        68%   { opacity: 1; }
        70%   { opacity: 0; }
        100%  { opacity: 0; }
      }
      .hub-glow-bright { animation: hubGlowAct2 12s ease infinite; }

      /* --- ACT 2: Bottom row dots — FAILURE --- */

      /* Bot request dot: agent(120) -> rest(450), starts at 6.7s */
      @keyframes botReq2 {
        0%    { cx: 120; opacity: 0; }
        55.8% { cx: 120; opacity: 0; }    /* 6.7s */
        56.5% { cx: 120; opacity: 1; }
        64%   { cx: 450; opacity: 1; }    /* 7.7s */
        65%   { cx: 450; opacity: 0; }
        100%  { opacity: 0; }
      }
      .bot-req2 { animation: botReq2 12s linear infinite; }

      /* Bot RED bounce dot: rest(450) -> agent(120), starts at 7.7s */
      @keyframes botBounce2 {
        0%    { cx: 450; opacity: 0; }
        64%   { cx: 450; opacity: 0; }    /* 7.7s */
        65%   { cx: 460; opacity: 1; }    /* slight overshoot */
        66%   { cx: 450; opacity: 1; }    /* bounce */
        71%   { cx: 120; opacity: 0.5; }  /* 8.5s */
        72%   { cx: 120; opacity: 0; }
        100%  { opacity: 0; }
      }
      .bot-bounce2 { animation: botBounce2 12s linear infinite; }

      /* Bot 404 label — Act 2 */
      @keyframes bot404Act2 {
        0%    { opacity: 0; }
        71%   { opacity: 0; }   /* 8.5s */
        73%   { opacity: 1; }
        91%   { opacity: 1; }   /* 11s */
        93%   { opacity: 0; }
        100%  { opacity: 0; }
      }
      .bot-404-2 { animation: bot404Act2 12s ease infinite; }

      /* Red X at REST box — Act 2 */
      @keyframes redXAct2 {
        0%    { opacity: 0; }
        64%   { opacity: 0; }   /* 7.7s */
        66%   { opacity: 1; }
        91%   { opacity: 1; }
        93%   { opacity: 0; }
        100%  { opacity: 0; }
      }
      .bot-red-x2 { animation: redXAct2 12s ease infinite; }

      /* --- AMBIENT ANIMATIONS --- */

      /* Hub subtle pulse */
      @keyframes hubPulse {
        0%    { transform: scale(1); }
        50%   { transform: scale(1.03); }
        100%  { transform: scale(1); }
      }
      .hub-group {
        transform-origin: 450px 140px;
        animation: hubPulse 4s ease-in-out infinite;
      }

      /* Dashed line flow */
      @keyframes dashFlow {
        0%   { stroke-dashoffset: 20; }
        100% { stroke-dashoffset: 0; }
      }
      .flow-line {
        stroke-dasharray: 8 4;
        animation: dashFlow 1.5s linear infinite;
      }
      .flow-line-dim {
        stroke-dasharray: 8 4;
        animation: dashFlow 2s linear infinite;
      }

      /* Speech bubble gentle pulse */
      @keyframes bubblePulse {
        0%   { opacity: 0.9; }
        50%  { opacity: 1; }
        100% { opacity: 0.9; }
      }
      .speech-bubble { animation: bubblePulse 3s ease-in-out infinite; }

      /* Hub glow idle pulse */
      @keyframes hubGlowIdle {
        0%   { opacity: 0.25; }
        50%  { opacity: 0.4; }
        100% { opacity: 0.25; }
      }
      .hub-glow-idle { animation: hubGlowIdle 3s ease-in-out infinite; }
    </style>
  </defs>

  <!-- Dark background -->
  <rect width="900" height="420" fill="#0d0f12" rx="12"/>

  <!-- =============================================
       TITLE
       ============================================= -->
  <text x="450" y="30" font-family="system-ui, -apple-system, sans-serif" font-size="26" font-weight="700" fill="white" text-anchor="middle">CogniMesh</text>

  <!-- Divider line between rows -->
  <line x1="30" y1="215" x2="870" y2="215" stroke="#1f2937" stroke-width="1" stroke-dasharray="4 4"/>

  <!-- =============================================
       TOP ROW — "With CogniMesh"
       ============================================= -->

  <!-- Section label -->
  <text x="40" y="75" font-family="system-ui, -apple-system, sans-serif" font-size="13" font-weight="700" fill="#8b7cf8">With CogniMesh</text>

  <!-- === AGENT (top row, centered at x=90, y=140) === -->
  <circle cx="90" cy="125" r="8" fill="none" stroke="#60a5fa" stroke-width="1.5"/>
  <path d="M77,150 a13,9 0 0,1 26,0" fill="none" stroke="#60a5fa" stroke-width="1.5" stroke-linecap="round"/>
  <text x="90" y="168" font-family="system-ui, -apple-system, sans-serif" font-size="10" fill="#9ca3af" text-anchor="middle">Agent</text>

  <!-- Speech bubble (top row) -->
  <g class="speech-bubble">
    <rect x="35" y="175" width="115" height="24" rx="7" fill="#1e293b" stroke="#374151" stroke-width="0.8"/>
    <polygon points="75,175 82,167 89,175" fill="#1e293b" stroke="#374151" stroke-width="0.8"/>
    <line x1="76" y1="175.5" x2="88" y2="175.5" stroke="#1e293b" stroke-width="1.5"/>
    <!-- Text A: Customer health? -->
    <text x="92" y="191" font-family="system-ui, -apple-system, sans-serif" font-size="9" fill="#93c5fd" text-anchor="middle" class="text-a">Customer health?</text>
    <!-- Text B: Revenue by region? -->
    <text x="92" y="191" font-family="system-ui, -apple-system, sans-serif" font-size="9" fill="#fcd34d" text-anchor="middle" class="text-b">Revenue by region?</text>
  </g>

  <!-- === CONNECTION LINES (top row) === -->
  <line x1="120" y1="140" x2="400" y2="140" stroke="#4b5563" stroke-width="1" class="flow-line"/>
  <line x1="500" y1="140" x2="750" y2="140" stroke="#4b5563" stroke-width="1" class="flow-line"/>

  <!-- === COGNIMESH HUB (centered at x=450, y=140) === -->
  <g class="hub-group">
    <!-- Idle glow -->
    <circle cx="450" cy="140" r="42" fill="url(#glowPurple)" class="hub-glow-idle"/>
    <!-- Bright glow for composing in Act 2 -->
    <circle cx="450" cy="140" r="50" fill="url(#glowPurpleBright)" class="hub-glow-bright"/>
    <!-- Rounded rect background -->
    <rect x="412" y="110" width="76" height="60" rx="14" fill="#1a1430" stroke="#8b7cf8" stroke-width="1.5" stroke-opacity="0.6"/>
    <!-- Mesh: central node + 6 surrounding -->
    <circle cx="450" cy="140" r="4" fill="#8b7cf8"/>
    <circle cx="436" cy="128" r="2.5" fill="#8b7cf8" fill-opacity="0.7"/>
    <circle cx="464" cy="128" r="2.5" fill="#8b7cf8" fill-opacity="0.7"/>
    <circle cx="432" cy="144" r="2.5" fill="#8b7cf8" fill-opacity="0.7"/>
    <circle cx="468" cy="144" r="2.5" fill="#8b7cf8" fill-opacity="0.7"/>
    <circle cx="442" cy="156" r="2.5" fill="#8b7cf8" fill-opacity="0.7"/>
    <circle cx="458" cy="156" r="2.5" fill="#8b7cf8" fill-opacity="0.7"/>
    <!-- Inner connection lines -->
    <g stroke="#8b7cf8" stroke-width="0.8" stroke-opacity="0.4">
      <line x1="450" y1="140" x2="436" y2="128"/>
      <line x1="450" y1="140" x2="464" y2="128"/>
      <line x1="450" y1="140" x2="432" y2="144"/>
      <line x1="450" y1="140" x2="468" y2="144"/>
      <line x1="450" y1="140" x2="442" y2="156"/>
      <line x1="450" y1="140" x2="458" y2="156"/>
      <!-- Outer ring -->
      <line x1="436" y1="128" x2="464" y2="128"/>
      <line x1="464" y1="128" x2="468" y2="144"/>
      <line x1="468" y1="144" x2="458" y2="156"/>
      <line x1="458" y1="156" x2="442" y2="156"/>
      <line x1="442" y1="156" x2="432" y2="144"/>
      <line x1="432" y1="144" x2="436" y2="128"/>
    </g>
  </g>
  <text x="450" y="182" font-family="system-ui, -apple-system, sans-serif" font-size="10" fill="#a78bfa" text-anchor="middle" font-weight="600">CogniMesh</text>

  <!-- "composing..." label near hub (Act 2 only) -->
  <text x="450" y="100" font-family="system-ui, -apple-system, sans-serif" font-size="9" fill="#c4b5fd" text-anchor="middle" font-style="italic" class="composing-label">composing...</text>

  <!-- === DATABASE (top row, centered at x=790, y=140) === -->
  <ellipse cx="790" cy="124" rx="24" ry="10" fill="#2dd4bf" fill-opacity="0.15" stroke="#2dd4bf" stroke-width="1.2"/>
  <rect x="766" y="124" width="48" height="30" fill="#2dd4bf" fill-opacity="0.08" stroke="none"/>
  <ellipse cx="790" cy="154" rx="24" ry="10" fill="#2dd4bf" fill-opacity="0.15" stroke="#2dd4bf" stroke-width="1.2"/>
  <line x1="766" y1="124" x2="766" y2="154" stroke="#2dd4bf" stroke-width="1.2"/>
  <line x1="814" y1="124" x2="814" y2="154" stroke="#2dd4bf" stroke-width="1.2"/>
  <ellipse cx="790" cy="138" rx="24" ry="10" fill="none" stroke="#2dd4bf" stroke-width="0.5" stroke-opacity="0.3"/>
  <text x="790" y="176" font-family="system-ui, -apple-system, sans-serif" font-size="10" fill="#9ca3af" text-anchor="middle" class="db-label-gold">Gold</text>
  <text x="790" y="176" font-family="system-ui, -apple-system, sans-serif" font-size="10" fill="#9ca3af" text-anchor="middle" class="db-label-silver">Silver</text>

  <!-- === ANIMATED DOTS — ACT 1 TOP === -->
  <!-- Blue request: agent -> hub -->
  <circle cy="140" r="5" fill="#60a5fa" class="top-req1"/>
  <!-- Teal pass-through: hub -> db -->
  <circle cy="140" r="5" fill="#2dd4bf" class="top-pass1"/>
  <!-- Green response: db -> agent -->
  <circle cy="140" r="5" fill="#4ade80" class="top-resp1"/>

  <!-- 200 OK label (Act 1) -->
  <g class="top-ok1">
    <rect x="40" y="90" width="52" height="18" rx="5" fill="#065f46" fill-opacity="0.8"/>
    <text x="66" y="103" font-family="system-ui, -apple-system, sans-serif" font-size="10" fill="#4ade80" text-anchor="middle" font-weight="700">200 OK</text>
  </g>

  <!-- === ANIMATED DOTS — ACT 2 TOP === -->
  <!-- Blue request: agent -> hub -->
  <circle cy="140" r="5" fill="#fcd34d" class="top-req2"/>
  <!-- Teal composed query: hub -> db -->
  <circle cy="140" r="5" fill="#2dd4bf" class="top-composed2"/>
  <!-- Green response: db -> agent -->
  <circle cy="140" r="5" fill="#4ade80" class="top-resp2"/>

  <!-- 200 OK + "composed from Silver" label (Act 2) -->
  <g class="top-ok2">
    <rect x="25" y="90" width="130" height="18" rx="5" fill="#065f46" fill-opacity="0.8"/>
    <text x="90" y="103" font-family="system-ui, -apple-system, sans-serif" font-size="10" fill="#4ade80" text-anchor="middle" font-weight="700">200 OK</text>
    <text x="90" y="118" font-family="system-ui, -apple-system, sans-serif" font-size="8" fill="#6ee7b7" text-anchor="middle">composed from Silver</text>
  </g>


  <!-- =============================================
       BOTTOM ROW — "REST endpoints"
       ============================================= -->

  <!-- Section label -->
  <text x="40" y="245" font-family="system-ui, -apple-system, sans-serif" font-size="13" font-weight="700" fill="#9ca3af">REST endpoints</text>

  <!-- === AGENT (bottom row, centered at x=90, y=310) === -->
  <circle cx="90" cy="295" r="8" fill="none" stroke="#60a5fa" stroke-width="1.5"/>
  <path d="M77,320 a13,9 0 0,1 26,0" fill="none" stroke="#60a5fa" stroke-width="1.5" stroke-linecap="round"/>
  <text x="90" y="338" font-family="system-ui, -apple-system, sans-serif" font-size="10" fill="#9ca3af" text-anchor="middle">Agent</text>

  <!-- Speech bubble (bottom row) -->
  <g class="speech-bubble" style="animation-delay: 0.5s">
    <rect x="35" y="345" width="115" height="24" rx="7" fill="#1e293b" stroke="#374151" stroke-width="0.8"/>
    <polygon points="75,345 82,337 89,345" fill="#1e293b" stroke="#374151" stroke-width="0.8"/>
    <line x1="76" y1="345.5" x2="88" y2="345.5" stroke="#1e293b" stroke-width="1.5"/>
    <!-- Text A: Customer health? -->
    <text x="92" y="361" font-family="system-ui, -apple-system, sans-serif" font-size="9" fill="#93c5fd" text-anchor="middle" class="text-a-bot">Customer health?</text>
    <!-- Text B: Revenue by region? -->
    <text x="92" y="361" font-family="system-ui, -apple-system, sans-serif" font-size="9" fill="#fcd34d" text-anchor="middle" class="text-b-bot">Revenue by region?</text>
  </g>

  <!-- === CONNECTION LINES (bottom row) === -->
  <line x1="120" y1="310" x2="400" y2="310" stroke="#374151" stroke-width="1" class="flow-line-dim"/>
  <line x1="500" y1="310" x2="750" y2="310" stroke="#1f2937" stroke-width="1" stroke-dasharray="3 6"/>

  <!-- === REST API BOX (centered at x=450, y=310) === -->
  <rect x="412" y="280" width="76" height="60" rx="10" fill="#1a1c22" stroke="#4b5563" stroke-width="1.2"/>
  <text x="450" y="315" font-family="system-ui, -apple-system, sans-serif" font-size="11" fill="#9ca3af" text-anchor="middle" font-weight="600">REST API</text>

  <!-- Red X at REST box (Act 2 only) -->
  <g class="bot-red-x2">
    <circle cx="450" cy="310" r="11" fill="#7f1d1d" fill-opacity="0.6"/>
    <path d="M444,304 L456,316" stroke="#f87171" stroke-width="2.2" stroke-linecap="round"/>
    <path d="M456,304 L444,316" stroke="#f87171" stroke-width="2.2" stroke-linecap="round"/>
  </g>

  <!-- "no endpoint" label (Act 2 only) -->
  <text x="450" y="356" font-family="system-ui, -apple-system, sans-serif" font-size="9" fill="#6b7280" text-anchor="middle" class="bot-404-2">no endpoint</text>

  <!-- === DATABASE (bottom row, same teal as top) === -->
  <ellipse cx="790" cy="294" rx="24" ry="10" fill="#2dd4bf" fill-opacity="0.15" stroke="#2dd4bf" stroke-width="1.2"/>
  <rect x="766" y="294" width="48" height="30" fill="#2dd4bf" fill-opacity="0.08" stroke="none"/>
  <ellipse cx="790" cy="324" rx="24" ry="10" fill="#2dd4bf" fill-opacity="0.15" stroke="#2dd4bf" stroke-width="1.2"/>
  <line x1="766" y1="294" x2="766" y2="324" stroke="#2dd4bf" stroke-width="1.2"/>
  <line x1="814" y1="294" x2="814" y2="324" stroke="#2dd4bf" stroke-width="1.2"/>
  <ellipse cx="790" cy="308" rx="24" ry="10" fill="none" stroke="#2dd4bf" stroke-width="0.5" stroke-opacity="0.3"/>
  <text x="790" y="346" font-family="system-ui, -apple-system, sans-serif" font-size="10" fill="#9ca3af" text-anchor="middle">Gold</text>

  <!-- === ANIMATED DOTS — ACT 1 BOTTOM === -->
  <!-- Blue request: agent -> REST -->
  <circle cy="310" r="5" fill="#60a5fa" class="bot-req1"/>
  <!-- Blue pass-through: REST -> db -->
  <circle cy="310" r="5" fill="#60a5fa" class="bot-pass1"/>
  <!-- Green response: db -> agent -->
  <circle cy="310" r="5" fill="#4ade80" class="bot-resp1"/>

  <!-- 200 OK label (Act 1, bottom) -->
  <g class="bot-ok1">
    <rect x="40" y="260" width="52" height="18" rx="5" fill="#065f46" fill-opacity="0.8"/>
    <text x="66" y="273" font-family="system-ui, -apple-system, sans-serif" font-size="10" fill="#4ade80" text-anchor="middle" font-weight="700">200 OK</text>
  </g>

  <!-- === ANIMATED DOTS — ACT 2 BOTTOM === -->
  <!-- Blue request: agent -> REST -->
  <circle cy="310" r="5" fill="#fcd34d" class="bot-req2"/>
  <!-- Red bounce-back: REST -> agent -->
  <circle cy="310" r="5" fill="#f87171" class="bot-bounce2"/>

  <!-- 404 label (Act 2, bottom) -->
  <g class="bot-404-2">
    <rect x="44" y="260" width="44" height="18" rx="5" fill="#7f1d1d" fill-opacity="0.8"/>
    <text x="66" y="273" font-family="system-ui, -apple-system, sans-serif" font-size="10" fill="#f87171" text-anchor="middle" font-weight="700">404</text>
  </g>

  <!-- =============================================
       FOOTER
       ============================================= -->
  <text x="450" y="412" font-family="system-ui, -apple-system, sans-serif" font-size="8" fill="#374151" text-anchor="middle">cognimesh.dev</text>

</svg>